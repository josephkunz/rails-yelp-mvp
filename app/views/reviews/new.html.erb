<h1>Add New Review Form</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :content, label: 'Add New Review' %>
  <%= f.input :rating, collection: Review::RATINGS %>
  <%= f.button :submit %>

<% end %>
